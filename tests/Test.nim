#Test. Runs every test.

import libTest
echo "Done with the lib tests."

import WalletTest
echo "Done with the Wallet tests."

import DatabaseTest
echo "Done with the Database tests."

import NetworkTest
echo "Done with the Network tests."

import SyntaxTest
